# AI4ACC 知识图谱 / 本体模块设计方案

> 版本：v0.2（设计稿，待评审）
> 定位：在现有「规则 + LLM」分类体系之上，增加一层**本体驱动的知识层**，解决商户归一化、长尾分类冷启动、消费关联分析三个核心问题。
> 设计参考：吸收 Palantir Foundry Ontology 的核心思想（语义层 + 动能层、Action Log 审计、Proposal 审批流），并按单用户小项目规模降级采用（详见 §11）。

---

## 1. 背景与问题

### 1.1 现状

当前系统（`data_loader.py` + `analyzer.py` + `tools.py` + `agent.py`）的工作方式是：

```
账单 → 规则匹配分类 → LLM 兜底分类 → 人工确认 → 写分类规则（upsert_rule）
```

### 1.2 四个核心痛点

| 痛点 | 表现 | 根因 |
| --- | --- | --- |
| P1 商户归一化缺失 | `星巴克`、`星巴克(成都春熙路店)`、`Starbucks` 在商户排行里是 3 行 | 只有字符串，没有实体概念 |
| P2 长尾分类冷启动 | 新商户每次都要靠 LLM 猜或人工确认 | 知识不沉淀，每次从零开始 |
| P3 规则是平铺的 | `MERCHANT_RULES` 是 keyword→category 字典，没有层级和关联 | 没有建模商户/品牌/行业的关系 |
| P4 无法做关联分析 | 问不出"健身和饮料消费的关联" | 数据无图结构 |

### 1.3 设计目标

1. **商户实体化**：把"字符串商户名"提升为"实体"，支持别名归一化、品牌聚合
2. **知识沉淀**：每一次分类确认都写回知识层，越用越聪明
3. **确定性推理**：可解释的推理链路（实体→品牌→行业→分类），LLM 只处理真正的未知
4. **零侵入集成**：不推翻现有架构，作为增强层叠加在 `analyzer.py` / `tools.py` / `app.py` 之上

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     交互层（复用现有）                        │
│   Web（新增「商户知识管理」页）    CLI    AI 问答 Agent         │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                本体层 = 语义层(Semantics) + 动能层(Kinetics)   │
│                                                            │
│  语义层（名词 World of Nouns）                               │
│    Object: 商户/品牌/行业/分类/交易/账户                       │
│    Property: 商户属性（行业/连锁/城市）                        │
│    Link: 交易-发生于-商户, 门店-隶属品牌-品牌, 商户-属于-分类    │
│                                                            │
│  动能层（动词 World of Verbs）  ← 借鉴 Palantir：动作与对象同层 │
│    Query Function（只读）: resolve_merchant / get_category…   │
│    Action（写操作）: confirm_entity / merge_batch_classify    │
│      每个 Action 定义：修改哪些对象 + 副作用 + 审计写入         │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                知识查询层 query.py（推理引擎）                 │
│   lookup_merchant / get_category / get_brand / classify_new   │
│   规则链推理（确定性、可解释）                                │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌───────────────┬──────────┴──────────┬───────────────────────┐
│  ontology.yaml │   instances.yaml    │  SQLite 知识表（主存储） │
│  （语义+动能定义）│  （实例，含提案态）  │  merchant_entities     │
│  TBox / Verbs  │   ABox              │  merchant_aliases      │
└───────────────┴─────────────────────┴───────────────────────┘
                           ▲
                           │ 写入（分类确认 / 离线抽取脚本）
              ┌────────────┴────────────┐
              │  Action Log（审计日志）   │  ← 所有写操作留痕
              └─────────────────────────┘
                           ▲
┌──────────────────────────┴──────────────────────────────────┐
│                    LLM 实体抽取（离线批量 + 在线沉淀）           │
│     只产出「提案(Proposal)」，经人确认后 merge 进主库          │
└─────────────────────────────────────────────────────────────┘
```

**存储策略**：
- SQLite 新增知识表作为**主存储**（与现有 `transactions` 等表共存，外键不强关联，保持解耦）
- `ontology.yaml` / `instances.yaml` 作为**模式定义与版本化文档**，启动时可选加载到内存，用于冷启动和人工维护
- 不用 Neo4j：个人数据量（<1 万条交易、<1 千商户）用图数据库是过度设计；复杂图算法（可选）用 NetworkX 加载即可

---

## 3. 本体模型设计（TBox，模式层）

### 3.1 概念体系（`ontology.yaml`）

```yaml
ontology:
  version: "0.1"
  classes:
    交易:                       # 核心节点，对应现有 transactions 表
      属性: [金额, 时间, 方向, 描述]
    商户:                       # 交易的发生对象
      子类:
        品牌商户:               # 星巴克、瑞幸——有统一品牌认知
        门店商户:               # 星巴克(成都春熙路店)
        个人:                   # 转账/还款对象
        组织:                   # 公司、机构
      属性:
        - 行业                   # 餐饮/交通/零售/教育/娱乐...
        - 人均消费               # 可选
        - 是否连锁               # bool
        - 城市
    支付渠道:
      子类: [微信, 支付宝, 兴业银行]
    账户:                       # 对应现有 accounts 表
    分类:                       # 直接复用现有分类体系（taxonomy 即本体）
      父子关系: 餐饮 > 咖啡, 餐饮 > 快餐, 交通 > 地铁 ...
    人:                         # 用户本人/家人/朋友/同事（转账对象归属）

  relations:
    - 交易 -发生于-> 商户
    - 交易 -通过-> 支付渠道
    - 交易 -属于-> 分类
    - 交易 -关联账户-> 账户
    - 门店商户 -隶属品牌-> 品牌商户
    - 商户 -属于行业-> 行业
    - 商户 -位于-> 城市
    - 品牌商户 -包含门店-> 门店商户    # 逆关系

  industries:                   # 行业枚举（用于推理和聚合）
    - 餐饮
    - 交通
    - 零售
    - 教育
    - 娱乐
    - 医疗
    - 住房
    - 通讯
    - 其他

  # ── 动能层（Kinetics）：动词定义，借鉴 Palantir「Action」概念 ──
  # 与语义层同源建模：每个 Action 声明「改了哪些对象、副作用、审计」
  actions:
    confirm_entity:             # 用户确认一个提案实体 → 主库
      输入: { proposal_id }
      修改对象: [merchant_entities, merchant_aliases]
      副作用: [删除/归档对应 proposal, 写 action_log]
    merge_batch_classify:       # 批量分类（改造现有 batch_classify_merchant）
      输入: { merchant, category, txn_ids }
      修改对象: [transactions(分类), category_rules, merchant_entities]
      副作用: [规则 upsert, 实体 upsert, 写 action_log]
    create_pending_entity:      # LLM 抽取产物，只写提案态（不碰主库）
      输入: { raw_name, suggested_category }
      修改对象: [pending_merchants]
      副作用: [写 action_log]

  query_functions:              # 只读计算函数（Agent 只能读，不能改）
    - resolve_merchant
    - get_merchant_category
    - get_brand_merchants
    - get_all_entities
    - get_related_merchants
```

**设计要点**：
- 分类体系是现成的本体骨架（taxonomy），`MERCHANT_RULES` 的每个 key 实际上是一条"商户→分类"断言
- `门店商户 → 品牌商户 → 行业 → 分类` 构成一条**推理链**，这是本设计的核心
- 领域很窄（个人财务），5 个概念、7 种关系足够，**不需要复杂 OWL 公理**，避免过度设计
- **「动词」与「名词」同层定义**（Palantir 核心思想）：改动世界的路径（Action）写在模式里，而不是散落在应用层——Agent 只能在声明过的 Action 内行动

### 3.2 实例层（ABox，`instances.yaml` 示例）

```yaml
instances:
  merchants:
    - name: 星巴克
      type: 品牌商户
      industry: 餐饮
      category: 餐饮/咖啡
      is_chain: true
      city: null
      aliases: [星巴克, Starbucks, 星巴克(成都春熙路店)]
    - name: 成都地铁
      type: 组织
      industry: 交通
      category: 交通/地铁
      aliases: [成都地铁, 成都轨道交通]
    - name: 王XX
      type: 个人
      relation: 家人          # 转账对象归属
  brands:
    - name: 瑞幸咖啡
      members: [瑞幸咖啡, 瑞幸, luckin coffee]
```

> 说明：实例的**主存储是 SQLite**，`instances.yaml` 用于版本化管理与可读性，两者双向可同步。

---

## 4. SQLite 知识表设计（主存储）

```sql
-- 商户实体表：每个真实商户一行
CREATE TABLE IF NOT EXISTS merchant_entities (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    name            TEXT NOT NULL UNIQUE,        -- 规范名（主名）
    entity_type     TEXT DEFAULT 'brand',        -- brand / store / person / org
    brand_id        INTEGER,                     -- 隶属品牌（自引用，门店→品牌）
    industry        TEXT DEFAULT '',             -- 行业
    category        TEXT DEFAULT '',             -- 关联分类（含父子，如 餐饮/咖啡）
    category_confidence TEXT DEFAULT 'high',     -- high / medium / pending
    is_chain        INTEGER DEFAULT 0,
    city            TEXT DEFAULT '',
    relation        TEXT DEFAULT '',             -- 个人/组织时：本人/家人/朋友/同事
    source          TEXT DEFAULT 'manual',       -- manual / llm / import
    status          INTEGER DEFAULT 1,           -- 1 有效 0 停用
    created_at      TEXT DEFAULT (datetime('now','localtime')),
    updated_at      TEXT DEFAULT (datetime('now','localtime')),
    FOREIGN KEY(brand_id) REFERENCES merchant_entities(id)
);

-- 别名表：所有出现过的商户名字符串 → 实体（归一化映射）
CREATE TABLE IF NOT EXISTS merchant_aliases (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    alias         TEXT NOT NULL UNIQUE,          -- 账单里的原始商户名
    entity_id     INTEGER NOT NULL,
    is_exact      INTEGER DEFAULT 1,             -- 精确匹配 / 模糊(包含)
    created_at    TEXT DEFAULT (datetime('now','localtime')),
    FOREIGN KEY(entity_id) REFERENCES merchant_entities(id)
);

-- 提案表：LLM/规则产出的待确认实体（Palantir Proposal 机制的降级实现）
-- 状态机：pending(提案) → confirmed(merge 进主库) / rejected / archived
CREATE TABLE IF NOT EXISTS pending_merchants (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    raw_name      TEXT NOT NULL,                 -- 原始商户名（账单里的字符串）
    suggested_category TEXT DEFAULT '',
    suggested_industry TEXT DEFAULT '',
    suggested_brand TEXT DEFAULT '',
    suggested_by   TEXT DEFAULT 'llm',           -- llm / rule / manual
    reason        TEXT DEFAULT '',               -- 判断依据（可解释）
    evidence      TEXT DEFAULT '',               -- 样本交易摘要（JSON）
    status        TEXT DEFAULT 'pending',        -- pending / confirmed / rejected / archived
    occurrence_count INTEGER DEFAULT 0,          -- 出现次数（越活跃越优先确认）
    created_at    TEXT DEFAULT (datetime('now','localtime')),
    confirmed_at  TEXT,
    merged_entity_id INTEGER,                    -- merge 后对应的主库实体
    FOREIGN KEY(merged_entity_id) REFERENCES merchant_entities(id)
);

-- 消费关联规则表（推理产物，Phase 4）
CREATE TABLE IF NOT EXISTS merchant_relations (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    from_entity   INTEGER NOT NULL,
    to_entity     INTEGER NOT NULL,
    relation_type TEXT NOT NULL,                 -- co_occur / brand_member / chain
    weight        REAL DEFAULT 0,                -- 关联强度
    rule          TEXT DEFAULT '',               -- 生成依据（可解释）
    created_at    TEXT DEFAULT (datetime('now','localtime'))
);

-- 动作审计日志（Palantir Action Log 概念的轻量落地）
-- 每次写操作（批量分类/打标签/确认实体/改规则）都留下一条不可变记录
CREATE TABLE IF NOT EXISTS action_log (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    action        TEXT NOT NULL,                 -- 动作名（对应 ontology.actions）
    target_type   TEXT NOT NULL,                 -- transactions / merchant_entities / category_rules ...
    target_ids    TEXT NOT NULL,                 -- 受影响对象 id 列表（JSON）
    detail        TEXT DEFAULT '',               -- 变更内容摘要（JSON：before → after）
    source        TEXT DEFAULT '',               -- 触发来源：agent / web / cli / script
    created_at    TEXT DEFAULT (datetime('now','localtime'))
);
```

索引：
- `merchant_aliases(alias)` 唯一索引——**查询热路径**
- `merchant_entities(brand_id)`、`merchant_entities(category)`

---

## 5. 查询接口设计（`query.py`）

新增 `agent/knowledge/query.py`，对外提供 6 个函数（全部同步、纯逻辑、可单测）：

```python
# 1. 实体解析：账单商户名 → 实体（归一化核心）
def resolve_merchant(raw_name: str) -> MerchantEntity | None

# 2. 取分类：任一商户名 → 分类（推理链主入口）
#    优先级：精确别名 > 品牌继承 > 行业映射 > 分类规则(MERCHANT_RULES) > None
def get_merchant_category(raw_name: str, rules: dict) -> tuple[str, str]  # (category, confidence)

# 3. 品牌聚合：某品牌的全部门店名（用于统计合并）
def get_brand_merchants(brand_id: int) -> list[str]

# 4. 全部实体清单（管理页 / 导出）
def get_all_entities() -> list[dict]

# 5. 新商户入库：未识别的商户 → 创建待确认实体（供 LLM 抽取脚本与确认页用）
def create_pending_entity(raw_name: str, category: str = "") -> int

# 6. 关联查询（Phase 4）：某商户的关联消费商户
def get_related_merchants(entity_id: int, limit: int = 10) -> list[dict]

# 7.（动能层 Action 实现）确认提案实体 → merge 进主库
#    对应 ontology.actions.confirm_entity，内部自动写 action_log
def confirm_entity(proposal_id: int, category: str) -> int

# 8. 查询待确认提案（确认页数据源）
def get_pending_proposals(limit: int = 50) -> list[dict]

# 9. 动作审计日志查询（管理页展示）
def get_action_log(action: str | None = None, limit: int = 100) -> list[dict]
```

调用方改造点（**向后兼容，原函数签名不变**）：

| 文件 | 改动 |
| --- | --- |
| `analyzer.get_top_merchants` | 统计前先 `resolve_merchant` 归一化，同实体合并金额与笔数（修 P1） |
| `data_loader._match_merchant` | 规则匹配前先查知识表（修 P2 冷启动） |
| `tools._batch_classify_merchant` | 写分类规则的同时，`upsert` 商户实体与别名 + **写 action_log**（知识沉淀 + 审计） |
| `app.py /api/merchants/batch-classify` | 同上（Web 侧对称改动） |
| `app.py` 各写操作 API | 统一走 `log_action()` 辅助函数（批量分类/改分类/打标签/规则启停） |
| `agent.py` SYSTEM_PROMPT | 增加一句："不确定分类时，可先调用商户知识查询；写操作必须先向用户确认" |

---

## 6. 实体抽取与知识沉淀

### 6.1 离线批量抽取（一次性建库，脚本 `agent/knowledge/export_entities.py`）

```
流程：全部交易 → 按商户名聚合 → 相似名聚类（归一化候选）
     → LLM 批量标注（行业/品牌/是否连锁/分类）→ 输出待确认列表
     → Web「商户知识管理」页人工确认 → merge 进主库
```

- 输入：`effective_transactions()` 的全部商户名
- 聚类规则：精确名优先；无精确名时用「品牌词提取」（如去掉括号/地点后缀后匹配）+ 相似度阈值（如 difflib ≥ 0.8）
- LLM 标注：复用 `app.py` 里已有的批量 prompt 模式（每批 30 商户，`json_object` 输出），字段：`industry / brand / is_chain / category`
- 输出：**提案**（存 `pending_merchants`，status=pending）。这是 Palantir「Proposal → Review → Merge」机制的降级版：**LLM 只能提议，确认权在用户**，确认后 `confirm_entity()` 才把数据 merge 进主库并写审计日志

### 6.2 在线知识沉淀（持续增强）

在现有"分类确认"流程中顺带写入，**零额外成本**：

- `batch_classify_merchant(商户X, 分类Y)` 执行时：
  - 若 `X` 不在 `merchant_aliases` → 插入别名 + 新建实体（category=Y, confidence=high）
  - 若已存在 → 更新 `updated_at`（活跃度）
- 用户手动改单笔分类时同样触发
- 以上每次写操作自动追加一条 `action_log`（谁/何时/改了哪些对象/变更前后）

### 6.3 新商户兜底链路

```
新商户名 → 知识表查（miss）→ MERCHANT_RULES 查（miss）→ LLM 建议
         → 用户确认 → 写回知识表（下次直接命中）
```

链路只比现在多一步"先查知识表"，LLM 调用量**不增反减**（因为命中后不再需要 LLM）。

---

## 7. 推理规则设计（确定性、可解释）

| 规则 | 逻辑 | 输入 | 输出 |
| --- | --- | --- | --- |
| R1 别名解析 | `alias` 精确/包含匹配 → 实体 | 商户字符串 | 实体 |
| R2 分类继承 | 实体.category 为空 → 取 brand.category → 取 industry 默认分类 | 实体 | 分类 + confidence |
| R3 父子分类继承 | 已有父分类 → 子分类未定 → 归入父分类（如"咖啡"→"餐饮"） | 分类 | 分类 |
| R4 品牌聚合 | 门店实体 brand_id 相同 → 统计合并 | 实体集合 | 聚合后的统计 |
| R5 固定支出辅助 | 同一实体每月出现且金额稳定 → 提示"可能是固定支出" | 实体 | 候选列表 |
| R6 关联挖掘（Phase 4） | 同账户、时间窗口内（如 2 小时）两商户共现次数 ≥ N → 写 `merchant_relations` | 交易集 | 关联规则 |
| R7 写操作审计（动能层约束） | 所有 Action 执行时强制写 `action_log`；**LLM 的写操作必须先经用户确认**（确认前只落提案表） | 写操作 | 审计记录 |

**置信度设计**：知识表命中 = high；规则推理 = medium；LLM = pending（保持现有三级，语义一致）。

**约束原则（借鉴 Palantir「AI 只能在定义范围内行动」）**：
- Agent 可调用 = `query_functions`（只读）+ 已声明的 `actions`（写，需用户确认）
- 超出声明的动作，Agent 无权执行——从机制上限制 LLM 越权和幻觉扩散

---

## 8. 实施计划（4 期）

### Phase 1 — 基础层（预估改动最小，可独立上线）
- [x] 本设计文档评审
- [ ] `agent/knowledge/ontology.yaml`：本体模式定义（含动能层 Actions 声明）
- [ ] SQLite 新增 5 张知识表（`database.py` 加建表 + 增删改查方法）：`merchant_entities` / `merchant_aliases` / `pending_merchants` / `merchant_relations` / `action_log`
- [ ] `agent/knowledge/query.py`：`resolve_merchant` / `get_merchant_category` / `get_pending_proposals` / `log_action` 等
- [ ] 单测：`tests/test_knowledge.py`（R1/R2/R3/R7 规则）

### Phase 2 — 知识沉淀接入（价值最快兑现）
- [ ] `tools._batch_classify_merchant` + `app.py` 批量分类接口：写规则时同步 upsert 实体 + 写 `action_log`
- [ ] `app.py` 各写操作 API 接入 `log_action()`（改分类/打标签/规则启停）
- [ ] `analyzer.get_top_merchants`：归一化聚合（修 P1，UI 商户排行立即可见效果）
- [ ] `data_loader._match_merchant`：先查知识表（修 P2 冷启动）

### Phase 3 — 实体抽取与确认（Proposal 机制）
- [ ] `export_entities.py`：LLM 离线批量抽取 → 只写提案表（pending）
- [ ] Web「商户知识管理」页：提案确认（`confirm_entity`）+ 实体/别名编辑 + action_log 查看
- [ ] 从现有真实数据跑一遍，人工确认全部商户，形成初始知识库

### Phase 4 — 关联分析（可选，亮点功能）
- [ ] R6 关联挖掘 + `get_related_merchants` 查询
- [ ] AI 问答增强：Agent 对话支持"我常去的餐饮品牌有哪些""健身完一般会买什么"（新增只读工具 `get_merchant_knowledge`）
- [ ] 消费画像：按品牌/行业维度的月度消费分布

---

## 9. 风险与边界

| 风险 | 应对 |
| --- | --- |
| 商户名聚类误合并（把两家店合成一家） | 仅"精确别名"和"人工确认"写入实体；LLM 聚类结果一律走确认页 |
| 知识表与规则表数据不一致 | 规则表保持权威（不改），知识表是增强层，`get_merchant_category` 先知识表后规则表 |
| 迁移历史数据成本 | Phase 3 一次性脚本 + 人工确认，不自动化写库 |
| 面试被问"为什么不用 Neo4j" | 明确回答：数据规模不匹配，SQLite + 内存图足够，图算法按需用 NetworkX——体现工程判断力 |
| 过度设计风险 | 领域固定（个人财务），本设计刻意把本体限制在 5 概念 / 7 关系，不引入 SWRL/OWL 推理器 |

---

## 10. 面试故事线（如何讲）

> **"我把记账系统从『规则 + LLM 的分类器』升级为『本体驱动的知识系统』，设计参考了 Palantir Foundry 的 Ontology 哲学：**
> 1. **建模**：定义了商户/品牌/行业/分类 4 类实体和 7 种关系，分类体系本身就是 ontology 的 taxonomy
> 2. **归一化**：所有账单商户名通过别名表解析到实体，修掉了商户排行统计分裂的 bug
> 3. **推理**：门店→品牌→行业→分类的链式继承，新商户不再每次依赖 LLM
> 4. **沉淀**：用户每次分类确认都写回知识层，系统越用越准——用 10 行规则替换了 60% 的 LLM 调用
> 5. **治理**：借鉴 Palantir 的"语义层+动能层"——动词（写操作）与名词（对象）同层定义，Agent 只能在声明过的 Action 内行动；所有写操作留 Action Log 审计；LLM 的实体抽取只产生提案（Proposal），必须经用户确认才能 merge 进主库，把 AI 的越权与幻觉限制在机制层面"

这段故事同时体现了：**实体建模、系统设计、LLM 与确定性规则的分工、数据闭环、工程治理意识（看过前沿实践且有取舍地落地）** 五种能力，是简历和面试里很加分的独立亮点。

---

## 11. 与 Palantir Foundry Ontology 的借鉴与取舍

本设计吸收了 Palantir Foundry Ontology 的三个核心思想，并按单用户小项目规模做了降级适配：

### 11.1 借鉴了什么

| Palantir 概念 | 原文要点 | 本项目落地 |
| --- | --- | --- |
| **语义层 + 动能层**（Nouns + Verbs） | 对象/属性/关系（名词）与 Action/Function/动态安全（动词）**在同一层建模**；数据模型包含"更新路径" | `ontology.yaml` 中同时声明对象、关系与 `actions` / `query_functions`；`tools.py` 的写工具 = Action 的实现，`analyzer.py` 查询 = Function |
| **Action Log 审计** | 每个 Action 提交都被建模为对象，永久记录"谁、何时、改了哪些数据" | `action_log` 表，所有写操作（分类/打标/确认实体/规则启停）强制留痕，可追溯 |
| **Proposal → Review → Merge** | 数据模式/业务规则变更走"分支提案 + 人工审批 + 合并主库"，杜绝影子变更 | `pending_merchants` 提案表：LLM/规则产出只写提案态，用户确认后 `confirm_entity()` 才 merge 进主库 |
| **语义墙约束 AI 幻觉** | AI 只读结构化本体、只在定义好的 Action 内行动，从机制上消除幻觉与越权 | Agent 可调用范围 = `query_functions`（只读）+ 已声明 `actions`（写，需用户确认）；写入前必须展示确认 |
| **数据即操作（Operational Layer）** | 数据不是"给人看的快照"，而是驱动业务的活层 | 分类确认 → 规则/实体沉淀 → 影响后续统计与预测，形成"越用越准"的闭环 |

### 11.2 明确不借鉴（防止过度设计）

| Palantir 组件 | 为什么不用 |
| --- | --- |
| OMS / OSS / Funnel / Object Storage 微服务 | 面向企业级分布式部署；本项目 SQLite + 内存规则即可 |
| Branch / 多级审批 / Approvals App | 单用户场景降级为「待确认列表」，无多级审批需求 |
| Restricted Views / MDO 列级权限 | 无多用户/多部门，不需要细粒度访问控制 |
| FDE / AI FDE 工作流 | 概念可借鉴（AI 产出需人审），但不引入团队协作流程 |
| OWL / SWRL 推理器 | 领域窄且固定，确定性规则链已覆盖全部推理需求 |

### 11.3 一句话总结

> **"Palantir 的启示是：本体不只描述世界，还要描述世界如何被改变。我按这个思路把系统改造成『名词定义 + 动词约束 + 全量审计 + 人审合并』，但控制在一个个人项目需要的复杂度内。"**

---

## 12. 待确认问题（评审时讨论）

1. 商户知识管理页是放在「设置」页内，还是独立导航？现有 UI 是纯手写 JS，是否接受新增一个页面
2. `ontology.yaml` 是否需要一开始就做"schema 校验"（如用 pydantic），还是先用注释规范
3. 是否需要 Phase 4 的关联挖掘（涉及时间窗口参数，需要真实数据调优），还是先只做 1-3 期
4. 知识表要不要暴露 REST API 供 AI Agent 直接调用（新增 `get_merchant_knowledge` 工具），还是先只内部使用
5. Action Log 是否需要 Web 端展示页面，还是先只入库、后续再加 UI
